; Namespace as struct...

(let module {
  open: {_: (fn () (io/write 1 "Called open\n")),
    spec: {
      documentation: "Do an module operation (open)."
      arity: 2
      type: ["string","string"]}
  }   
  close: {_: (fn () (io/write 1 "Called close\n")), spec: {
    documentation: "Do an module operation (close)."
    arity: 0
    type: []}
  }
})

(io/write 1 ($ "%s\n" module.open))
(io/write 1 ($ "%s\n" module.close))
(module.open)
(module.close)

; Change internal struct construction...

; (let identity {name: "", login: "", password:""})
